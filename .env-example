# ========================================
# Exemplo de Variáveis de Ambiente
# Sistema de Agendamento de Veículos
# ========================================
# 
# INSTRUÇÕES:
# 1. Este arquivo é apenas um exemplo (não edite)
# 2. O arquivo .env é criado automaticamente pelo setup.sh
# 3. Edite .env apenas se precisar personalizar
# 4. NUNCA commite o arquivo .env (está no .gitignore)
#

# ===========================================
# CONFIGURAÇÕES BÁSICAS
# ===========================================

# Chave secreta do Django (OBRIGATÓRIA)
SECRET_KEY=sua-chave-secreta-super-segura-aqui-mude-em-producao

# Modo de debug (True=desenvolvimento, False=produção)
DEBUG=True

# Hosts permitidos (separados por vírgula)
ALLOWED_HOSTS=localhost,127.0.0.1

# ===========================================
# CONFIGURAÇÕES REGIONAIS
# ===========================================

# Fuso horário
TIME_ZONE=America/Sao_Paulo

# Código do idioma
LANGUAGE_CODE=pt-br

# ===========================================
# CONFIGURAÇÕES OPCIONAIS (descomente se necessário)
# ===========================================

# Banco de dados customizado
# DATABASE_URL=postgres://usuario:senha@localhost:5432/nome_do_banco

# Email (para recuperação de senha)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=seu-email@gmail.com
# EMAIL_HOST_PASSWORD=sua-senha-app